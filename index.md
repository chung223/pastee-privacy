---
title: 隱私政策
---

<!-- 這是原始檔。改完請跑 ./scripts/publish_privacy.sh 推到公開版：
     https://chung223.github.io/pastee-privacy/ （上架表單填的就是那個網址） -->

# 貼貼 隱私政策

最後更新：2026-09-20

貼貼是剪貼簿管理工具。它會保存你複製的內容，所以我們把「資料到底在哪、什麼時候會離開你的裝置」寫清楚。

**開發者沒有伺服器。** 貼貼沒有帳號、沒有登入、沒有分析工具、沒有第三方 SDK。我們收不到你的剪貼內容，也收不到你怎麼使用這個 App 的統計，因為根本沒有地方可以收。

---

## 資料存在哪裡

你的剪貼歷史存在你自己的裝置上：

| 平台 | 位置 |
| --- | --- |
| iPhone / iPad | App Group 容器內的資料庫，主 App、貼貼鍵盤、分享擴充功能與小工具共用 |
| Mac | App 沙盒容器內的資料庫 |

圖片原檔存在同一個容器的 `Blobs/` 資料夾，縮圖存在資料庫裡。

以下處理**全部在你的裝置上完成**，不經過任何伺服器：

- **圖片文字辨識（OCR）** — Apple 的 Vision 框架
- **語意搜尋** — Apple 的 NLEmbedding 句向量
- **Apple Intelligence 功能**（摘要、翻譯、校對、改寫、自然語言找剪貼）— Apple 的裝置端基礎模型
- **敏感內容偵測**（卡號、API 金鑰、私鑰、JWT）— 本機比對
- **Spotlight 索引** — 系統本機索引

## 資料什麼時候會離開你的裝置

只有三種情況，每一種都有開關，而且預設狀態列在下面。

### 1. iCloud 同步（預設開啟）

開啟時，剪貼會同步到**你自己的 iCloud 私人資料庫**（Apple 的伺服器，你的帳號）。開發者沒有存取權限。

- 文字類欄位以 CloudKit 的**端對端加密欄位**（`encryptedValues`）儲存，金鑰在你的裝置上，Apple 也讀不到內容
- 圖片與大型富文字以 CloudKit 資產上傳，可在設定中單獨關閉圖片同步
- 同步使用 Apple 推播服務喚醒裝置抓取變更。推播只帶「有變更」的訊號，不含剪貼內容

關閉方式：設定 › 透過 iCloud 同步。

### 2. 連結預覽（預設開啟）

存入網址時，貼貼會連到**那個網址所在的網站**抓取標題與網站圖示。這代表該網站會看到一次來自你裝置的請求。

- 被標記為敏感的剪貼不抓取
- 一次性連結、或會因為被存取而失效的連結，可能會受影響

關閉方式：設定 › 抓取連結標題與網站圖示。

### 3. 區網 Peer Share（預設關閉）

開啟後，同一個 Wi-Fi 下、輸入相同配對碼的裝置之間可以直接互傳剪貼。

- 傳輸走 Apple 的 MultipeerConnectivity，端對端加密
- **不經過任何伺服器**，也不經過 iCloud
- 配對碼不相符的裝置會被拒絕連線

關閉方式：設定 › Peer Share（預設就是關的）。

---

## 貼貼鍵盤與「允許完整取用」

貼貼鍵盤需要你在系統設定中開啟「允許完整取用」。這裡說明為什麼，以及它實際做了什麼。

**為什麼需要**：iOS 的鍵盤擴充功能在沒有完整取用時，無法存取 App Group 的共用容器。貼貼鍵盤的唯一功能就是顯示你的剪貼歷史讓你選取，讀不到那個資料庫就完全沒有作用。

**它做什麼**：

- 讀取共用容器裡的剪貼資料庫
- 把你選取的那一筆輸入到游標處
- 選擇圖片時，寫入系統剪貼簿（因為無法直接插入圖片）
- 讀取游標前已經輸入的文字，用來篩選清單（可在設定中關閉）

**它不做什麼**：

- 不記錄你的按鍵
- 不把你輸入的任何內容傳送出去
- 不連接任何網路服務

貼貼鍵盤的 entitlement 只有 App Group 一項，沒有任何網路相關權限。

---

## 診斷報告

貼貼使用 Apple 的 MetricKit 接收系統產生的當機、卡頓與 CPU 例外報告。

- 報告由系統交給 App，**留在你的裝置上**
- **不會自動送出**
- 你可以在設定 › 診斷報告中查看，並自行決定要不要分享給開發者

此外，如果你在系統設定中開啟了 Apple 的「分享分析與改進功能」，當機報告會依 Apple 的政策送給 Apple，開發者可透過 Apple 的開發者工具看到匿名化的當機統計。這是 Apple 的機制，可在系統設定中關閉。

---

## 隱私保護機制

貼貼預設就會避開一些內容：

- **忽略密碼管理員**：1Password、Bitwarden 等 App 複製的內容預設不記錄
- **尊重系統標記**：遵守 `org.nspasteboard.ConcealedType`（機密）與 `TransientType`（暫存）標記
- **自動遮罩**：偵測到信用卡號、API 金鑰、私鑰、JWT 時仍會儲存但以遮罩顯示
- **敏感內容自動過期**：可設定 N 分鐘後自動刪除
- **上鎖收藏集**：內容不出現在任何列表，需 Touch ID／Face ID 才能檢視
- **螢幕擷取保護**：錄影、鏡像或螢幕分享時，Mac 的面板不會進入畫面，iOS 會遮蔽內容
- **密碼欄位暫停**（Mac）：系統安全輸入模式啟用時暫停擷取

## 你要求的權限

| 權限 | 平台 | 用途 |
| --- | --- | --- |
| 輔助使用 | Mac | 模擬 ⌘V 把剪貼直接貼進其他 App。未授權時只會複製到剪貼簿 |
| 相機 | iOS | 文件掃描。掃描的影像存成剪貼並在裝置上辨識文字 |
| 本地網路 | 兩者 | 僅在開啟 Peer Share 時使用 |
| 允許完整取用 | iOS 鍵盤 | 見上方說明 |

## 刪除你的資料

- **單筆**：在列表上滑動或右鍵刪除；iOS 可用編輯模式多選後批次刪除
- **自動清理**：設定 › 保留規則，可依筆數或天數自動刪除較舊的項目（釘選與收藏集內的不受影響）
- **iCloud 上的資料**：設定 › 刪除 iCloud 資料，會清掉私人資料庫中的貼貼區域
- **全部**：移除 App。裝置上的資料庫會跟著一起刪除

移除 App 會連同裝置上的資料庫一起刪除。iCloud 上的副本需另外刪除。

## 聯絡方式

有隱私相關問題請透過 App Store 上架頁面的支援連結聯絡。

---

*貼貼的程式中只有三處會對外連線：連結預覽、iCloud 同步，以及區網 Peer Share。除此之外沒有任何網路請求。*
